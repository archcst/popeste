# Popaste 产品需求

状态：需求与已确认交互  
更新日期：2026-09-22

## 1. 产品定位

Popaste 是一个小巧的 macOS 常用文本工具，主要用于保存和快速插入常用提示词。

核心体验：在任意支持粘贴的输入位置，按快捷键呼出小浮窗，通过键盘选择提示词，确认后插入原来的输入位置，继续工作。

面向个人使用，强调操作简短、键盘友好、界面精致，以及符合苹果原生应用的使用习惯。

## 2. 核心使用场景

- 在聊天输入框中插入代码审查、需求分析、翻译、润色等常用提示词。
- 在编辑器、笔记或网页表单中插入多行文本模板。
- 临时新增或修改提示词，保存后立即在浮窗中找到并使用。
- 全程使用键盘完成呼出、查找、选择、插入和关闭。

## 3. 第一版需求

### 3.1 全局快捷键

- 用户可以在设置中录制和修改呼出快捷键。
- 设置中清楚展示当前快捷键。
- 无效或无法使用的组合应给出明确提示；保留原来的有效设置。
- 修改后的快捷键立即生效，重新启动应用后仍然保留。
- 浮窗已打开时，再次按呼出快捷键应收起浮窗。
- 默认快捷键在产品试用时确定，用户始终可以修改。

### 3.2 浮窗位置与行为

- 呼出时优先显示在文字插入光标附近，方便用户保持视线和输入上下文。
- 无法获得文字插入位置时，在鼠标附近显示，仍允许选择并粘贴。
- 浮窗完整显示在当前相关屏幕范围内，靠近屏幕边缘时调整位置。
- 尽量避开正在输入的那一行文字。
- 呼出、关闭及取消操作应保留原应用的输入位置和选区。
- 按 Esc 或点击浮窗外部时关闭，不修改原输入内容。
- 同一时刻只显示一个选择浮窗。

### 3.3 提示词列表与查找

- 每行直接显示一条提示词的正文开头，长内容单行省略，支持按需预览完整正文。
- 默认显示常用内容；支持置顶，置顶内容优先展示。
- 呼出后可以直接输入文字搜索，匹配正文。
- 支持中文、英文及多行正文。
- 搜索时即时更新结果，清楚标示当前选中项。
- 没有结果时展示明确的空状态；没有任何提示词时提供创建入口。
- 第一版默认使用平铺列表；分组是否需要进入第一版，在实际条目数量增加后再确定。

### 3.4 键盘操作

必须支持 Ctrl+N/P/F/B 风格的导航，用户无需把手移到方向键区域。

| 操作 | 需求 |
| --- | --- |
| Ctrl+N / Ctrl+P | 选择下一项 / 上一项，搜索框有焦点时同样可用 |
| 方向键上 / 下 | 与列表上下选择行为一致 |
| Ctrl+F / Ctrl+B | 提供向前 / 向后的操作；具体对应左右切换、预览或分组导航，需在交互定稿时明确 |
| Enter | 确认当前条目并插入 |
| Esc | 关闭浮窗并返回原输入场景 |
| 直接输入文字 | 搜索提示词 |

- Ctrl+F/B 的最终行为必须清楚、一致，并避免与搜索框内的文本编辑行为产生歧义。
- 当前选中项变化时，应保持其在可见范围内。
- 无结果或无选中项时，Enter 不产生粘贴操作。
- 中文输入法组词期间，用于确认候选词的 Enter 不应误触发提示词插入。

### 3.5 插入文本

- 选中提示词并确认后，将完整正文粘贴到呼出前的输入位置，然后关闭浮窗。
- 原应用中存在选中文本时，遵循该应用正常粘贴的替换行为。
- 保留正文中的换行、缩进、空行、中文、特殊符号和 Emoji。
- 默认插入纯文本，避免携带不需要的字体、颜色等格式。
- 一次确认只插入一次，不重复、不截断。
- 插入完成后用户可以继续在原输入框中打字。
- 仅插入内容，不自动发送消息或提交表单。
- 无法直接插入时明确提示，并提供复制正文的替代操作。
- 正常插入应尽量保留用户原有剪贴板内容，且不覆盖操作期间用户新复制的内容。

### 3.6 创建和管理

- 提供容易发现的提示词管理入口和创建入口。
- 支持创建、编辑、删除提示词，以及置顶和取消置顶。
- 每条提示词保存正文，支持多行长文本，适合完整提示词。
- 空正文不能保存，并说明需要补充什么。
- 编辑既有内容时，明确区分保存和取消；取消不影响已保存版本。
- 未保存内容关闭时应有保护，避免误操作丢失。
- 删除操作应有确认或可撤销能力。
- 创建或编辑保存后，选择浮窗立即使用最新内容。
- 支持提示词导入、导出，供备份和迁移使用；导入前说明新增和重复正文的处理方式，避免静默覆盖。

### 3.7 设置与日常使用

- 设置中可以修改全局呼出快捷键。
- 支持选择是否开机启动。
- 日常保持轻量常驻，通过菜单栏提供呼出、管理、设置和退出入口。
- 关闭管理或设置窗口后，仍可以通过全局快捷键使用应用。
- 首次使用时清楚说明必要的系统权限及其用途，并提供前往设置的入口。
- 权限缺失时说明受影响的功能，用户仍可以管理和复制提示词。
- 提示词及配置保存在 `~/.config/popeste`，分别使用 `prompts.json` 与 `config.json`；迁移旧数据时保留原始备份。重新启动后完整保留，日常使用无需账号和网络。

## 4. 视觉与体验要求

- 苹果原生风格，整体简洁、克制，避免信息拥挤。
- 浮窗小巧，默认容纳约 6–8 条提示词；更多条目通过滚动查看。
- 使用清晰的文字层级、适度圆角、轻柔阴影及原生感材质。
- 支持系统浅色和深色外观，保证文字及选中状态清晰可辨。
- 常见操作以键盘为主，也支持鼠标点击。
- 创建、编辑和设置界面采用已确认的 Codex 风格：冷浅灰侧栏、白色内容区、轻边框与圆角。
- 管理与设置固定为设计稿中档尺寸；快捷浮窗可选择小（80%）、中（90%）、大（100%），文字、控件与间距同步缩放。
- 提示词正文较长时不撑大整个选择浮窗；完整编辑在管理界面完成。
- 呼出、筛选和选择应流畅，无明显等待或闪烁。
- 尊重系统减少动态效果及相关辅助使用偏好。

## 5. 验收标准

- 用户创建一条多行提示词后，能在其他应用的输入框中通过快捷键呼出并找到它。
- 用户可以用 Ctrl+N/P 连续选择列表条目，用 Enter 完整插入，用 Esc 无副作用地取消。
- Ctrl+F/B 有明确、可预测的最终行为，并符合交互定稿的约定。
- 修改全局快捷键后，新快捷键立即可用，重启后设置保留。
- 在可以识别文字光标的场景中，浮窗出现在其附近；其余场景按约定回退，并保持可操作。
- 多显示器和屏幕边缘场景下，浮窗可见且可完整操作。
- 中文输入法确认候选词不会误插入或误发送提示词。
- 插入长文本、换行、缩进和 Emoji 时内容准确，且不会触发消息发送或表单提交。
- 创建、编辑、置顶、删除及导入导出均有清楚反馈，重启后数据保持一致。
- 在用户常用的浏览器输入框、编辑器、笔记和聊天应用中验证完整使用流程，记录兼容性限制。

## 6. 交互定稿前待明确

- Ctrl+F/B 的具体选择行为，以及搜索框内的左右移动规则。
- 默认呼出快捷键。
- 第一版需要重点适配的应用名单及最低 macOS 版本。
- 是否需要在第一版加入分组，以及列表是否记住上次选择位置。

