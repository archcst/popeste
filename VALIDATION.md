# 光标上方优先（2026-09-24）

- 呼出时优先位于文字光标上方，上方不足才回退下方；鼠标回退沿用相同规则。
- 上方浮窗展开时保持底边、向上增长，下方浮窗保持顶边、向下增长；屏幕边缘仍执行可见范围约束。
- 定位回归覆盖上方优先、顶部空间不足回退、向上展开及收起还原。

# 空编辑器光标边界（2026-09-24）

- 优先使用单字符及文本标记的行边界，避免直接采用零长度范围返回的整块输入区域。
- 控件明确报告字符数为零、选区为零且边界高于 40 点时，使用顶部 22 点的首行估算；已有精确光标与非空选区不应用该估算。
- 原生回归覆盖空编辑器矩形、正常光标和非空选区；全部回归通过。Codex 空输入框截图提示此问题，但本次独立 AX 查询未取得其焦点元素，实际效果待用户验证。

# 浮窗光标定位与收起高度（2026-09-24）

- 收起状态按搜索框实际高度定位，取消预留完整列表高度；展开优先保持顶边，空间不足时向屏幕内移动，不再把列表压缩为搜索框高度。
- 获取插入位置时补充系统焦点回退、焦点子控件及父控件、文本标记范围与空范围兼容查询；对支持的应用请求启用辅助功能树。
- 定位测试覆盖鼠标在屏幕下半部时搜索框贴近锚点、展开尺寸与边界、足够空间时保持顶边；完整回归测试通过。
- 外部应用的真实光标定位尚待验收；无法取得有效文本几何时仍回退鼠标。Ghostty 等应用可能不提供完整文本范围接口。

# 玻璃外观继承试验（2026-09-24）

- 玻璃内容根视图不再显式覆盖 appearance，保留外层用户外观选择；文字使用系统语义色，标签交由 AppKit 绘制。
- Regular 暂时移除手工衬底，Clear 保留原衬底。大小选择器的选中文字放入玻璃 contentView，并校正布局顺序。
- Release 构建、完整回归测试、36 个原生布局预览通过。补充验证内容外观继承、无玻璃时的外观回退、胶囊文字层级与尺寸。
- 独立示例窗口对照浅/深外观与黑/白背景四种组合：Regular 材质会变化，但大面板仍保持所选主题；深色外观叠白底呈灰色，不能据此声称已解决所有背景下的可读性。
- 对照使用同窗口内的黑白背景，未等同于主应用覆盖其他应用的验收。草稿处理后已正常退出并重启主应用，切换至 Regular（磨砂玻璃）供实际背景试用。

# macOS 14+ 兼容与玻璃选项回退（2026-09-23）

- Swift Package 部署目标及应用 LSMinimumSystemVersion 统一为 macOS 14.0；Release 产物的 LC_BUILD_VERSION minos 为 14.0。
- NSGlassEffectView 保留 macOS 26 availability 检查，产物中该类为 weak external 引用，旧系统使用普通实色容器。
- 根据实际玻璃能力显示设置选项；不可用时隐藏整行并上移后续设置，保持用户已保存的玻璃样式。
- 原生测试覆盖三档尺寸下的选项显隐、后续行位置、实色背景，以及 POPASTE_GLASS=0 的真实容器回退。现有全部回归测试及 Release 构建通过。
- 本次验证环境为 macOS 26；macOS 14、15 实机启动与使用仍待验证。

# Liquid Glass 初步试验（2026-09-23）

- 稳定的 Swift 界面版本已标记为 `0.0.2`（b92bbd1）。玻璃效果基于该标签继续开发。
- macOS 26+ 使用单层 NSGlassEffectView regular 材质承载整个浮窗，保持布局与原生阴影；搜索和正文区域透明，列表选中态使用半透明底色。
- 列表文本尾部改用 alpha 蒙版渐隐，避免用实色覆盖玻璃背景。菜单和确认框维持实色以保证文字可读性。
- 启动时遇到旧系统、减少透明度，或环境变量 POPASTE_GLASS=0 时使用实色背景。macOS 27 专有交互效果尚未启用。
- macOS 26 实际启动检查收起搜索框及展开设置页。Release 构建与现有回归测试通过；未验证 macOS 27 实机效果。

---

# 全页面视觉核对（2026-09-23）

- 使用浏览器读取已确认 HTML 的实际布局，核对搜索、列表、详情、编辑、设置和保存确认。
- 统一设计稿色板与原始线性图标；修正搜索框左边距、列表正文留白、底栏 10 点提示、标题字重、置顶图标、预览返回提示底色和按钮尺寸。
- 正文恢复 25.5 点行高，详情与编辑共用排版参数；字符统计与 Vim 模式按设计稿放在同一行，确认框恢复 420×148.15 点及随文案计算的按钮间距。
- `scripts/render-native-preview.sh` 使用独立示例数据生成 6 状态 × 3 大小 × 2 外观，共 36 张原生预览到 `.build/ui-review`；检查了浅深色、三档大小的代表性预览，并对照浏览器的编辑页截图。预览不读取或修改用户数据。
- 存储、原生交互和设计稿参考测试通过；Release 构建通过。实际启动检查搜索、空白 Vim 编辑页和设置页。
- 这是布局和视觉核对，未进行逐像素一致性断言；浏览器与 TextKit 的字形栅格化仍由各自系统决定。

---

# 设置页设计稿还原（2026-09-23）

- 对照 compact HTML 的设置页 CSS：恢复 57 点顶栏、39 点设置行、18 点左右留白和 45 点底栏。
- 恢复顶部 Popaste、带边框快捷键录制按钮、带底板大小分段控件、30×18 开关、绿色权限圆点、紧邻标题的灰色配置路径、底部左侧保存提示和右侧完成按钮。
- 下拉改为浮窗内的原生绘制菜单，支持勾选、复选、鼠标悬停、方向键选择和 Esc 关闭。
- 原生测试覆盖开关双向切换、菜单键盘选择与收起；实际窗口检查设置页和快捷键菜单。Release 构建通过。

---

# 原生界面对齐（2026-09-23）

- 标签与按钮统一使用相同文字度量，搜索输入框垂直居中；标题、设置行和底栏共用各自行的中心线。
- 下拉箭头和插入 / 保存回车图标使用独立 SF Symbols，按图标与文字组合宽度居中。
- 实际检查搜索、设置、空编辑页、未保存弹窗及无匹配列表；测试草稿已丢弃。原生回归测试和 Release 构建通过。

---

# Swift 原生界面（2026-09-23）

- 搜索、列表、详情、编辑、设置和确认框迁移到 Swift / AppKit；正文使用 NSTextView / TextKit，构建产物不再链接 WebKit 或包含 HTML 资源包。
- 原生测试覆盖 emoji / 中文字素移动与删除、撤销、整行删除粘贴、模式切换、真实 TextKit 自动折行移动、保存成功 / 失败 / 丢弃 / 返回，以及搜索展开后的焦点与 marked text 键盘隔离。
- 实际窗口检查：连续搜索输入及结果数、中文和 emoji 正文、Vim 光标反色与折行移动、未保存对话框、原生设置菜单和小档尺寸。测试草稿已丢弃，Vim 设置恢复原值。
- 8 组存储与配置测试、原生界面测试、21 项设计稿参考测试、Release 构建通过。
- 保留已有数据格式和插入服务。本次未重新向外部应用粘贴，也未在实体中文输入法的候选窗中逐项验收；marked text 行为通过原生测试覆盖。macOS 13–25 尚未进行实机回归。

---

# Vim 显示行移动（2026-09-23）

- j/k 与上下方向键按 textarea 实际折行位置移动，支持数字次数并保留横向目标位置；越出可视区时滚动跟随。
- 实际窗口验证一条无换行文本在三个显示行间下移和上移；测试草稿已丢弃。
- 新增显示行目标定位与计数转发测试，10 项 Vim 测试及 Release 构建通过。

---

# Vim 编辑与未保存确认（2026-09-23）

- 独立 Vim 编辑开关默认关闭；实际验证普通/插入模式转换、两次 Esc 弹出未保存确认、Esc 保留草稿、N 丢弃测试草稿，验证后关闭开关。
- 新增 8 项 Vim 行为测试，覆盖 Unicode 字符、按行移动、删除/复制/粘贴、撤销/重做、连续输入与输入法隔离。
- 新增 5 项退出确认测试，覆盖回车保存成功后退出、失败重试、N 丢弃、Esc 保留、空正文保存失败。
- Swift 配置持久化测试与 Release 构建通过。

---

# 快捷键方案（2026-09-23）

- 增加方向键 / Emacs / Vim 复选，默认仅启用方向键；组合选择与空选择的配置持久化回归通过。
- 实际切换 Vim 并验证 Ctrl+J 展开与下移、Ctrl+K 上移、Ctrl+L 预览、Ctrl+H 返回；搜索展开与预览返回提示同步切换。
- 8 组 Swift 测试、6 项输入法事件测试、Release 构建通过。

---

# 搜索框收起与展开（2026-09-23）

- 默认搜索框高度 57 点，随三档大小缩放；展开保持窗口顶部位置，屏幕可用空间不足时限制列表高度。
- 实际验证向下键展开并选中第一条、输入关键词展开筛选、清空后保持展开、重新呼出收起，以及快捷键直接进入新建和设置。
- Release 构建、8 组 Swift 测试及 6 项输入法事件回归测试通过。

---

# 多语言扩展（2026-09-23）

- 未配置语言时默认跟随系统，按系统偏好顺序匹配 8 种界面语言，包含繁体中文地区识别。
- 补齐新增语言的全部界面翻译键及原生菜单、提示，语言菜单可滚动以适配紧凑浮窗。
- 实际验证日语界面及原生菜单切换，随后设置为跟随系统，正确匹配当前中文系统。
- 8 组 Swift 测试、6 项输入法测试、翻译键覆盖检查及 Release 构建通过。

---

# 设置与语言（2026-09-23）

- 新增简体中文、English、跟随系统选项；实际切换中英文，确认设置文字和原生菜单同步更新，已恢复中文。
- 外观和语言统一使用细线箭头下拉菜单，设置入口改为滑杆图标。
- 配置文件、灰色目录路径和打开按钮合并一行；移除提示词备份入口及导入导出消息处理。
- 实际点击打开后，访达显示 ~/.config/popeste 内的 config.json 与 prompts.json，浮窗收起。
- Debug / Release 构建通过；8 组 Swift 测试（含语言持久化）和 6 项输入法事件测试通过。

---

# 失焦收起（2026-09-23）

- 新增窗口 resign-key 与工作区应用切换监听，失焦时收起浮窗，不激活原应用，保留编辑草稿。
- 导入导出和退出确认期间暂缓失焦处理；实际验证导出对话框保持打开，取消后回到设置页。
- Debug / Release 构建通过；实体 Cmd+Tab 切换仍待用户试用确认。

---

# 左右方向键（2026-09-23）

- 右方向键进入选中项全文预览，左方向键从预览返回列表，与 Ctrl+F/B 对应；实际程序已验证。
- 仅处理无修饰键的左右键，正文编辑和中文组词期间仍保留输入行为。

---

# 选择页精简（2026-09-23）

- 选择页底栏提供新建、条目数和设置入口；每行悬停时显示编辑按钮，键盘聚焦也可访问。
- 实际应用验证 Command+N 新建、Command+E 编辑选中项、Command+, 打开设置；行内编辑打开对应正文。
- Debug / Release 构建、设计稿和运行脚本语法检查通过，6 项输入法搜索回归测试通过。

---

# 中文组词搜索修复（2026-09-23）

- 搜索使用独立的已提交文本；compositionstart 至 compositionend 期间的输入不触发筛选，后台刷新也不会读取拼音临时文本。
- compositionend 后筛选；重复的最终 input 事件不重复更新。组词期间将方向键和回车交给输入法。
- 6 项 JavaScript 事件回归测试通过：中文上屏、组词期间未标记 input、取消选字、连续组词、普通输入/粘贴/删除、重新呼出重置。
- 测试模拟浏览器输入法事件序列；真实输入法候选窗口未由自动化工具操作验证。

---

# 紧凑浮窗实现验证（2026-09-23）

- 按确认的 `design/popaste-compact-preview.html` 生成应用资源，列表、正文编辑和设置共用一个 NSPanel / WKWebView。
- 保留快捷键、存储位置与原有正文；配置和提示词继续存于 `~/.config/popeste`。
- 实际应用已验证：编辑入口、保存回列表、取消修改确认、收起后草稿保留、Ctrl+F/B 预览返回、搜索无匹配时 Enter 不插入、设置内缩放、深色显示、导出系统面板及取消返回。
- 最终版已验证浮窗内外观菜单可选择并恢复跟随系统，小/中/大布局完整显示；恢复原小档。构建资源一致性和签名检查通过。
- 8 组 Swift 测试通过，新增草稿不提前写入、保存保持 ID 和使用次数、空正文保存失败仍保留原数据的检查。
- 完整跨应用插入、真实中文输入法组词、多显示器实体环境和登录启动仍按下方矩阵验收。

以下是先前版本的记录；窗口尺寸与界面结构以本节和 README 为准。

---

# 当前界面补充验证（2026-09-22）

- 三个窗口通过 WKWebView 直接复用确认的 HTML/CSS；AppKit 负责系统交互。
- 修复重复打包导致资源目录嵌套、应用读取旧界面的问题；包内 HTML 与生成源码一致，签名验证通过。
- 当前界面已实测保存、管理搜索、设置搜索、浮窗全文预览/返回、无结果时回车保持浮窗、Esc 关闭。
- 三档浮窗实际截图尺寸：小 408×328、中 459×369、大 510×410，验证后恢复原小档。管理与设置固定 1080×720，使用中档样式。
- Release 构建、JavaScript 语法检查和 7 组数据/配置/位置测试通过。
- 当前版本完整跨应用粘贴及中文输入法组词尚未重新验收；以下记录中的原生控件实现描述属于之前版本。

---

# 当前实现验证（2026-09-22）

## 本次改造

- 按已确认 HTML 设计实现原生冷灰侧栏、白色编辑区、圆角卡片和单色主按钮。
- 每条提示词以正文呈现；管理界面直接编辑正文；管理和设置固定中档尺寸。
- 浮窗三档为 80%、90%、100%，设置保存到 `~/.config/popeste/config.json`。
- 提示词迁移到 `~/.config/popeste/prompts.json`，保存原始迁移备份。

## 本次已验证

- Debug、Release 编译通过，生成并启动 `dist/Popaste.app`。
- 7 组测试通过：屏幕边界与多屏坐标、文本精确保真与 CRUD、正文搜索排序、导入导出去重、非法备份拒绝、旧数据迁移/快捷键迁移/三档尺寸持久化、写入失败保持内存。
- 实际迁移后的正文与备份逐条一致；新数据版本 2，快捷键 ⌥A 保留，配置文件权限 0600。
- 原生 UI 观察：管理列表和编辑区显示完整迁移内容；设置有小/中/大选项；小档浮窗尺寸与字号缩小，正文单行显示。
- 新版设置实际显示辅助功能已授权。此前用户已确认过全局呼出及插入成功；此次数据迁移与视觉改造后的完整跨应用流程尚未重新逐项验收。
- 管理与设置均已截图检查。中、大档几何和配置重载有自动测试；实际跨应用与多显示器仍需按下方原验收矩阵验证。

---

以下为此前版本的历史验收记录，涉及标题字段与旧存储位置的条目仅代表当时版本。

# 验证记录

环境：本机 macOS arm64，Swift 6.4 / Command Line Tools。最低部署目标 macOS 13。

## 已完成

- Debug 和 Release 原生应用编译；生成 `dist/Popaste.app` 并本地签名。
- 独立 Swift 测试：多行、缩进、空行、中文和 Emoji 的持久化保真；创建、修改、删除及重新加载。
- 搜索标题及正文、多关键词、英文大小写匹配、置顶排序。
- 导入正文去重、重名改名、导入预览不修改原数据、导出再读取。
- 空标题/正文、损坏 JSON、未来版本、重复 ID 拒绝。
- 写入失败不改变内存中已保存数据。
- 应用启动及首次设置窗口：当前快捷键、登录启动开关、辅助功能说明、系统设置入口显示正常。
- 用户修改为 ⌥A 后多次重启仍保留；用户授权时界面正确显示已授权。
- 原生 UI 创建、保存、重启后读取多行中文/空行/缩进/Emoji。
- 原生 UI 验证正文预览、Ctrl+B 返回、无匹配结果 Enter 不插入。
- 原生 UI 验证取消修改确认及已保存正文恢复。
- 截图检查并修复正文预览叠字、管理按钮重叠。
- 位置算法测试覆盖主屏、负坐标副屏、上方副屏、小屏、四边和中心；浮窗完整位于可用区域，空间足够时避让输入行。
- 修复缩小浮窗时搜索/列表/预览/操作区跟随布局，以及鼠标选中列表后继续保持搜索输入焦点。
- 每次呼出明确重置第一项选择，与交互约定一致。
- 新版原生 UI 实测：鼠标选中列表条目后输入“第一行”，焦点仍在搜索框，正文匹配结果正确。

- 用户实测确认：重新授权后，使用 ⌥A 呼出、选择提示词并 Enter 插入成功。

## 需要实际权限与外部环境验证

用户移除旧条目并重新添加当前应用后，刷新设置已显示“辅助功能：已授权”。此前开关开启但 AXIsProcessTrusted 返回 false，是旧授权指纹与当前临时签名不匹配；此问题已通过重新添加解决。自动化工具向 TextEdit 发送 ⌥A 会产生字符 å，未触发 Carbon 全局快捷键；随后用户通过实体键盘完成 ⌥A 呼出、选择提示词并 Enter 插入，明确反馈“成功了”。此项为用户实测确认，目标应用及逐字符内容未独立核对。未开启登录启动。以下不能视为已通过：

| 场景 | 验收操作 |
| --- | --- |
| TextEdit / 原生文本框 | 选中一段文字，呼出搜索并 Enter，确认仅替换选区且继续输入正常 |
| Chrome / Safari 网页输入框 | 多行、缩进、Emoji 完整插入；页面不提交 |
| Cursor / VS Code | 编辑器插入、Ctrl+N/P 连续选择、滚动可见、焦点恢复 |
| Notes | 原生笔记中粘贴内容和取消选区保持 |
| Lark / 聊天应用 | 仅插入不发送；如自绘控件无法读取光标，应回退鼠标 |
| 中文输入法 | 拼音组词期间 Enter 确认候选词，下一次 Enter 才插入 |
| 多显示器 | 上下/左右排列、不同缩放、屏幕四边及菜单栏/Dock 附近可见 |
| 剪贴板竞争 | 确认后 800ms 内复制其他文本，新内容不得被恢复覆盖 |
| 权限撤销 | 回退复制流程；管理和备份仍可用 |
| 登录启动 | 安装到 Applications 后开启，退出登录再登录验证 |
| 快捷键持久化 | 录制新键后立即切换，重启保持；冲突组合提示并保留原值 |
| 外观与辅助使用 | 浅色/深色、高对比度、减少透明度、VoiceOver 实测 |

限制：AX 光标位置取决于目标应用的辅助功能实现；对不暴露该信息的控件使用鼠标位置。模拟粘贴无法通用检测外部应用是否真正接受，或是否延迟读取剪贴板；提供复制替代路径。对于未知备份格式会拒绝导入。应用只生成本机架构，尚未正式签名/公证。

## 按需求章节核对

| 需求 | 实现位置 / 证据 | 尚需实际验证 |
| --- | --- | --- |
| 3.1 全局快捷键 | HotKey.swift 注册新键成功后替换旧键；UserDefaults；⌥A 重启保持 | 真实全局键事件、冲突键与立即切换 |
| 3.2 位置与取消 | Insertion.swift AX 选区边界；Placement.swift 六组测试之一；Picker.swift 非激活浮窗、外部鼠标与 Esc | 原应用选区保持、实际多显示器与全屏空间 |
| 3.3 搜索列表 | Store.swift 标题/正文多关键词匹配，置顶/使用次数排序；UI 空状态与预览已验证 | 大量条目滚动体验 |
| 3.4 键盘 | Picker.swift Ctrl+N/P/F/B、方向键、Enter、Esc；检查 hasMarkedText；无结果 Enter 已验证 | 中文 IME、连续导航和鼠标选择后的键盘搜索 |
| 3.5 插入 | Insertion.swift 焦点核对、纯文本剪贴板、仅 Command+V、changeCount 保护；Picker busy 防重复 | 多应用真实插入、慢读取剪贴板和复制竞争 |
| 3.6 管理 | Manager.swift 创建/编辑/置顶/删除确认/草稿保护/备份；数据测试与编辑取消 UI 验证 | 导入导出系统面板整体验收 |
| 3.7 常驻设置 | 菜单栏、SMAppService、权限说明、原子本地存储；启动和关闭窗口代码路径 | 登录重启、权限重新授予后插入 |
| 4 视觉体验 | 原生 AppKit、系统材质/语义颜色、固定默认尺寸与滚动、无自定义动画；截图检查 | 深色与辅助使用偏好、VoiceOver |
| 5 应用兼容验收 | 上方矩阵列出原生文本/浏览器/编辑器/笔记/聊天应用 | 受辅助功能权限及实际应用环境约束，未宣称通过 |
| 6 待定交互 | README 确定 Ctrl+F/B、默认 ⌃⌥Space、macOS 13、平铺、每次重置选择；支持用户自定义快捷键 | 用户试用反馈可调整默认值 |
